@model int[]

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container mt-5">
    <div class="noi-dung-chinh">
        <h2 class="mb-4">Bảng Điều Khiển</h2>

        <!-- Thông báo -->
        <div class="alert alert-info alert-dismissible fade show" role="alert">
            Chào mừng bạn đến với bảng điều khiển quản trị.
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Đóng"></button>
        </div>

  @*       <div class="row">
            <div class="col-md-4 mb-3">
                <div class="card text-white bg-primary">
                    <div class="card-body">
                        <h5 class="card-title">Người Dùng</h5>
                        <p class="card-text">120 người dùng đã đăng ký</p>
                    </div>
                </div>
            </div>

            <div class="col-md-4 mb-3">
                <div class="card text-white bg-success">
                    <div class="card-body">
                        <h5 class="card-title">Vai Trò</h5>
                        <p class="card-text">5 vai trò được cấu hình</p>
                    </div>
                </div>
            </div>

            <div class="col-md-4 mb-3">
                <div class="card text-white bg-warning">
                    <div class="card-body">
                        <h5 class="card-title">Quyền Hạn</h5>
                        <p class="card-text">25 loại quyền truy cập</p>
                    </div>
                </div>
            </div> *@

            <div class="card mt-4">
                <div class="card-body">
                    <h5 class="card-title">Thống kê người dùng theo tháng</h5>
                    <canvas id="bieuDoNguoiDung"></canvas>
                </div>
            </div>

        </div>
    </div>
</div>
@section Scripts {
    <script>
        const ctx = document.getElementById('bieuDoNguoiDung').getContext('2d');
        const bieuDoNguoiDung = new Chart(ctx, {
            type: 'bar',
            data: {
                labels: ['Tháng 1', 'Tháng 2', 'Tháng 3', 'Tháng 4', 'Tháng 5', 'Tháng 6', 'Tháng 7', 'Tháng 8', 'Tháng 9', 'Tháng 10', 'Tháng 11', 'Tháng 12'],
                datasets: [{
                    label: 'Người dùng đăng ký',
                    data: [@Model[0], @Model[1], @Model[2], @Model[3], @Model[4], @Model[5], @Model[6], @Model[7], @Model[8], @Model[9], @Model[10], @Model[11]],
                    backgroundColor: '#0d6efd',
                    borderRadius: 5
                }]
            },
            options: {
                responsive: true,
                plugins: {
                    legend: {
                        display: false
                    },
                    title: {
                        display: false
                    }
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        title: {
                            display: true,
                            text: 'Số lượng người dùng'
                        }
                    },
                    x: {
                        title: {
                            display: true,
                            text: 'Tháng'
                        }
                    }
                }
            }
        });
    </script>
}


